name: Build src dists for PQMagic-Python

on:
  release:
    types: [published]

jobs:
  build:
    strategy:
      matrix:
        os: [ubuntu-latest, macos-latest]
        python-version: ["3.11"]
      fail-fast: false

    runs-on: ${{ matrix.os }}
    steps:
    - uses: actions/checkout@v4
    
    - name: Set up Python ${{ matrix.python-version }}
      uses: actions/setup-python@v4
      with:
        python-version: ${{ matrix.python-version }}
    
    - name: Install build tools
      run: |
        python -m pip install --upgrade pip
        pip install setuptools wheel Cython
        if [ "$RUNNER_OS" == "Linux" ]; then
          sudo apt-get update && sudo apt-get install -y cmake build-essential
        elif [ "$RUNNER_OS" == "macOS" ]; then
          brew install cmake
        fi

    - name: Build src dist
      run: python setup.py sdist

    # Error: ModuleNotFound. Weirdly, these tests work locally but not in the CI environment.
    #- name: Test the source distribution locally
    #  run: |
    #    python -m pip install dist/*.tar.gz
    #    python -c "import pqmagic; print(pqmagic.__version__)"
    #    python -c "from pqmagic import Kem; from binascii import hexlify; kem = Kem('ML_KEM_512'); pk, sk = kem.keygen(); print(hexlify(pk).decode('utf-8')); print('\n'); print(hexlify(sk).decode('utf-8'))"
    #    python tests/pypqmagic_kem_tests.py
    #    python tests/pypqmagic_sig_tests.py
        
    - name: Upload artifacts
      uses: actions/upload-artifact@v4
      with:
        name: wheels-${{ matrix.os }}-py${{ matrix.python-version }}
        path: dist/*